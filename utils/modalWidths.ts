/**
 * App-wide widths for form overlays: prioritize horizontal space so fields are
 * easier to scan and fill (not tall narrow strips).
 */
export const WIDE_MODAL_WIDTH: string | number = "min(960px, calc(100vw - 32px))";

/** Side drawers used for forms (e.g. expenses). */
export const WIDE_DRAWER_WIDTH: string | number = "min(640px, calc(100vw - 24px))";
